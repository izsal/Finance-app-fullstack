'use client'
import { useState } from 'react'
import Link from 'next/link'
import { useRouter } from 'next/navigation'
import { authClient } from '@/lib/auth-client'
export function AuthForm({ mode }: { mode: 'sign-in' | 'sign-up' }) { const signup = mode === 'sign-up'; const router = useRouter(); const [name,setName]=useState(''); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [error,setError]=useState(''); const [loading,setLoading]=useState(false); async function submit(e: React.FormEvent){ e.preventDefault(); setLoading(true); setError(''); const result = signup ? await authClient.signUp.email({ name,email,password }) : await authClient.signIn.email({ email,password }); setLoading(false); if(result.error){ setError('Email atau password tidak valid.'); return } router.push('/'); router.refresh() } return <main className="flex min-h-screen items-center justify-center bg-[#f7f9fb] p-6"><form onSubmit={submit} className="w-full max-w-md rounded-3xl border border-slate-200 bg-white p-8 shadow-xl"><div className="mb-8"><p className="text-sm font-bold text-teal-700">Dompetku</p><h1 className="mt-3 text-3xl font-bold text-slate-900">{signup ? 'Buat akun baru' : 'Selamat datang kembali'}</h1><p className="mt-2 text-sm text-slate-500">{signup ? 'Mulai kelola keuanganmu dengan lebih tenang.' : 'Masuk untuk melihat dashboard keuanganmu.'}</p></div>{signup && <label suppressHydrationWarning className="mb-4 block text-sm font-semibold text-slate-700">Nama<input value={name} onChange={e=>setName(e.target.value)} required className="mt-2 w-full rounded-xl border border-slate-200 p-3" /></label>}<label suppressHydrationWarning className="mb-4 block text-sm font-semibold text-slate-700">Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required className="mt-2 w-full rounded-xl border border-slate-200 p-3" /></label><label suppressHydrationWarning className="block text-sm font-semibold text-slate-700">Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} minLength={8} required className="mt-2 w-full rounded-xl border border-slate-200 p-3" /></label>{error && <p role="alert" className="mt-4 text-sm text-rose-600">{error}</p>}<button disabled={loading} className="mt-6 w-full rounded-xl bg-teal-700 py-3 font-bold text-white disabled:opacity-60">{loading ? 'Memproses...' : signup ? 'Daftar sekarang' : 'Masuk'}</button><p className="mt-6 text-center text-sm text-slate-500">{signup ? 'Sudah punya akun? ' : 'Belum punya akun? '}<Link className="font-bold text-teal-700" href={signup ? '/sign-in' : '/sign-up'}>{signup ? 'Masuk' : 'Daftar'}</Link></p></form></main> }
